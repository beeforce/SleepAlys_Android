<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sa.sleepanalysis">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.VIBRATE" />


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name="com.example.sa.sleepanalysis.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
                
        <!-- <receiver android:name=".AlarmReceiver" -->
        <!-- android:excludeFromRecents="true" -->
        <!-- /> -->

        <activity android:name="com.example.sa.sleepanalysis.AlarmScreen" />
        <activity android:name="com.example.sa.sleepanalysis.MainActivity" />
        <activity android:name="com.example.sa.sleepanalysis.Main2Activity" />
        <activity android:name="com.example.sa.sleepanalysis.signUpActivity" />
        <activity android:name="com.example.sa.sleepanalysis.ForgotPassword" />
        <activity android:name="com.example.sa.sleepanalysis.EditProfile"></activity>
    </application>

</manifest>